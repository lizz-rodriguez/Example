{
  "name": "database-structure-generator",
  "version": "1.0.0",
  "description": "A web application that allows users to upload documents or point to directories to automatically generate database structures for website creation",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint . --ext .js",
    "fix": "eslint . --ext .js --fix",
    "prettier": "prettier . --write",
    "sanity": "npm run lint && node --check app.js && node --check server.js && node --check schemaGenerator.js"
  },
  "keywords": [
    "database",
    "schema",
    "generator",
    "website",
    "automation",
    "file-upload"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "eslint": "^9.34.0",
    "nodemon": "^3.0.1",
    "prettier": "^3.6.2"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
